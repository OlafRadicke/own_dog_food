---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: site-apologie
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: site-apologie
  sources:
    - path: argocd-app-of-apps/cluster/irish-sea/site-apologie/manifests
      repoURL: https://github.com/OlafRadicke/own_dog_food.git
      targetRevision: HEAD
      directory:
        recurse: true

  destination:
    namespace: site-apologie
    server: https://kubernetes.default.svc
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
      allowEmpty: true
    syncOptions:
      - CreateNamespace=true
