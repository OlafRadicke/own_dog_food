---

kind:                       PersistentVolumeClaim
apiVersion:                 v1
metadata:
  name:                     uptime-kuma
  namespace:                pulumi-apps
spec:
  accessModes:
    -                       ReadWriteOnce
  storageClassName:         local-storage-uptime-kuma
  resources:
    requests:
      storage:              100Mi
